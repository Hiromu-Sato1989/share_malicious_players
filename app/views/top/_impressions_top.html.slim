.col-md-4
  .border.border-dark.rounded.mt-3.mb-3
    #impressions_top
      .text-center
        h4.sawarabi 閲覧数トップ5
      table.table.table-striped 
        thead.thead-default.text-center
          tr
            th= "Fighter's ID"
            th= "閲覧数"
        tbody.text-center
          - Fighter.number_of_impression.each do |fighter|
            tr
              td= link_to fighter.name, fighter_path(fighter), class: 'default-link-color'
              td= fighter.impressions_count
